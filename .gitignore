# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (generated, but we commit platforms/ for convenience)
# If you prefer pure generated artifacts, uncomment:
# platforms/
# .claude-plugin/
# .cursor-plugin/

# Dist / release artifacts
dist/
*.zip

# MCP server build output (source is in src/mcp/asc/src/, dist is built)
src/mcp/asc/dist/
src/mcp/asc/node_modules/

# Per-CLI local skill install / runtime artifacts (regenerated by install.sh or
# materialized by the CLI itself — disposable, never hand-edit). The committed
# copies are src/ (truth) and platforms/ (build cache) only.
# NOTE: AGENTS.md / GEMINI.md are tracked — they are generated FILES, not dirs.
.claude/
.agents/
.codex/
.agy/
.gemini/
skills-lock.json

# IDE
.vscode/
.idea/

# Logs
*.log
