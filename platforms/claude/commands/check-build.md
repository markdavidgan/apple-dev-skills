---
description: "Check CI build status and debug failures via App Store Connect MCP."
argument-hint: "[app]"
---

Run the `asc-build-check` skill on `$ARGUMENTS`.

Checks the latest CI build status, diagnoses failures, and verifies signing/provisioning.
