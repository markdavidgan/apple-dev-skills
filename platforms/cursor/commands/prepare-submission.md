---
description: "Check submission readiness, metadata, screenshots, and signing via ASC MCP."
argument-hint: "[app]"
---

Run the `asc-submission` skill on `$ARGUMENTS`.

Verifies App Store submission readiness including metadata, screenshots, signing, and TestFlight distribution.
