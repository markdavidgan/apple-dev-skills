---
description: "Diagnose and fix Swift 6 strict concurrency, Sendable, or MainActor isolation errors."
argument-hint: "[file]"
---

Run the `swift6-concurrency` skill on `$ARGUMENTS` (default: current directory).

Analyzes Swift 6 concurrency errors and applies the correct fix pattern.
