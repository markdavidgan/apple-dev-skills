---
description: "Complete a feature with full validation across build, tests, lint, and patterns. Runs the complete-feature skill."
argument-hint: "[feature-name]"
---

Run the `complete-feature` skill on `$ARGUMENTS`.
