---
description: "Full Apple-grade review (design + engineering + compliance + keynote). Review only, no fixes. Runs the apple-review skill."
argument-hint: "[app-dir]"
---

Run the `apple-review` skill on `$ARGUMENTS` (default: current directory).

Produce the comprehensive four-panel audit. Review only — no automated fixes.
