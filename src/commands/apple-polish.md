---
description: "Design & keynote craftsmanship review → select issues → fix → TestFlight. Runs the apple-polish skill."
argument-hint: "[app-dir]"
---

Run the `apple-polish` skill on `$ARGUMENTS` (default: current directory).

Follow the skill's full pipeline: file manifest → Design + Keynote review subagents → interactive selection → orchestration plan → parallel implementation squads → verification → TestFlight push.
