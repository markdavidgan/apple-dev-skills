---
description: "Write a failing regression test, fix the bug, verify, and check for similar issues."
argument-hint: "[bug-description]"
---

Run the `regression-test` skill for `$ARGUMENTS`.

Follows the full regression test workflow: identify bug class → write failing test → fix bug → verify → pattern check.
